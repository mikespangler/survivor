# Custom Fonts

Place your custom font files here:

## Required Font

- `Survivant.ttf` - Display font for hero headings and buttons

## Usage

After placing the font file, the theme will automatically load it via `@font-face` declarations in the global CSS.
